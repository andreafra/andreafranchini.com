---
import { getCollection } from "astro:content"
import Entry from "./TimelineItem.astro"

let allItems = await getCollection("works")

allItems = allItems
	.filter((a) => !a.data.hidden)
	.sort((a, b) => b.data.publishDate.getTime() - a.data.publishDate.getTime())
---
